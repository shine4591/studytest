2、为什么在一个事务中存在错误，Redis不回滚？

Redis一般很少会出现运行时错误，redis认为一般这种错误都是程序开发人员的错误，应当在开发期间解决，不应该留给运行时处理。
同时没有事务功能，将减小redis的复杂性，使其更快，更简单。


3. Redis不是只有一个线程吗？它已经卡死了，怎么接受script kill指令的？

lua 脚本引擎提供了各式各样的钩子函数，它允许在内部执行指令时执行钩子代码。如执行了N条业务指令后，再执行一次某个钩子函数。
因而script kill指令并不能立即kill掉正在执行的函数，存在延迟现象

